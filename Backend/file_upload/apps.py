from django.apps import AppConfig


class FileUploadConfig(AppConfig):
    """
    Represents the configuration for the `file_upload` app.
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "file_upload"
